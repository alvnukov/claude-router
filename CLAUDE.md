# claude-router

- Skills by phase: `.claude/skill-map.md`. The SessionStart hook in
  `.claude/settings.json` injects it on startup, /clear and compaction.
- Go navigation uses the happ MCP server; it must be enabled for this project:
  /mcp → happ → Enable.
